# AI-Assisted .gitignore Generator

Suggests appropriate entries for .gitignore based on the project's detected languages and frameworks.

## Free
```yaml
- uses: walshd1/ai-assisted-git-ignore-generator@v1
  with:
    gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
```

## Paid (cost + 4.75%)
```yaml
- uses: walshd1/ai-assisted-git-ignore-generator@v1
  with:
    service_token: ${{ secrets.ACTION_FACTORY_TOKEN }}
```
